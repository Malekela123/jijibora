name: eco_clean_mobile_app
description: "A new Flutter project."
publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: '>=3.11.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter

  cupertino_icons: ^1.0.6
  firebase_core: ^3.10.0      
  firebase_auth: ^5.4.0       
  cloud_firestore: ^5.6.0     
  firebase_storage: ^12.3.0   
  flutter_svg: ^2.0.10
  ionicons: ^0.2.2
  flutter_map: ^8.3.0
  latlong2: ^0.9.1
  image_picker: ^1.2.2
  geolocator: ^13.0.2         
  http: ^1.2.0                # Nimekuwekea hii kwa ajili ya ile Search API ya ramani

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0

flutter:
  uses-material-design: true

  assets:
    - assets/
    - assets/icons/
    - assets/imges/